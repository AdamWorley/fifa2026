{
  "_comment": "Manual corrections for matches the live API gets wrong or is missing (e.g. card counts). Each entry overrides the matching match by home+away team name. Leave matches empty to rely entirely on the API.",
  "matches": []
}
